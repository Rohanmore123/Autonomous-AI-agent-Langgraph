apiVersion: 1

providers:
  - name: llm-platform
    orgId: 1
    folder: LLM Platform
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards